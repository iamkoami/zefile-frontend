# Cloudflare Pages Configuration for ZeFile Frontend
# This file configures deployment to Cloudflare Pages with OpenNext

name = "zefile"
compatibility_date = "2025-12-25"
pages_build_output_dir = ".open-next/worker"

# Build command for Cloudflare Pages dashboard:
# npm run build && npx open-next build

# Custom domains will be configured in Cloudflare Pages dashboard
# Primary: zefile.io
# Staging: demo.zefile.io (optional)

[env.production]
# Production environment
vars = { }

[env.preview]
# Preview/staging environment
vars = { }
